1) if the character count for any of these text files exceeds 2048 characters, the embed won't send and the bot will just log an error

2) even the slightest syntax errors will cause the embed not to send

3) github is slow and sometimes it can take even 10-15 minutes before the raw content in the text files refresh
